@model IEnumerable<DeThiThuLan1.Models.KhachHang>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tuoi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DiaChi)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tuoi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DiaChi)
            </td>
            <td>
                @Html.ActionLink("Edit", "Update", new { id=item.ID }) |
                @Html.ActionLink("Details", "Details", new { id=item.ID }) |
                <form asp-action="Delete" asp-controller="KhachHang" asp-route-id = "@item.ID" method="post">
                    <button type="submit">Delete</button>
                </form>
                    <form asp-action="RollBack" asp-controller="KhachHang" asp-route-id="@item.ID" method="post">
                    <button type="submit">RollBack</button>
                </form>
            </td>
        </tr>
}
    </tbody>
</table>
